html
	head
		title Mi primera página web
	body
		- var arreglo = [1,2,3,4,5];
		- for (var i = arreglo.length - 1; i >= 0; i--)
			// Imprimir html con codigo js 
			// p!= "<h1>" + arreglo[i] + "</h1>"
			// Imprimir un texto literal sin interpretar html
			// p= "<h1>" + arreglo[i] + "</h1>"
			// Imprimir html mesclado con js
			// p Hola #{arreglo[i]}
			// Anidar etiquetas html
			p.
				Hola haz click en #[a(href="http://codigofacilito.com") este link] por favor
		// el if tambien se puede utilizar con - if pero si se usa asi la condicion va entre parentecis
		if true
			h1 Hola entre al if
		each item in arreglo
			p= item
			case item
				when 1
					p El item era uno
				when 2
					p El item era dos
				default
					p El item no era ni uno, ni dos